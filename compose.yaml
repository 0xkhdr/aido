services:
  app:
    build: .
    ports:
      - "8090:8080"
    environment:
      ADDR: ":8080"
      DB_PATH: "/app/data/app.db"
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
