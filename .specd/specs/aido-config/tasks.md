# Tasks — aido-config

Verify baseline for every code task (`workflow.md` W7): `CGO_ENABLED=0 go build ./... && go vet ./... && go test ./...`.

| id | role | files | depends-on | verify | acceptance | refs | kind | risk | complexity | capabilities | context | evidence | checks |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ✅ T1 | craftsman | go.mod, internal/config/paths.go, internal/config/paths_test.go | - | CGO_ENABLED=0 go build ./... && go vet ./... && go test ./... | R1.1, R1.2, R1.3 | R1.1, R1.2, R1.3 | feature | medium | standard | read,write | .specd/steering/tech.md, .specd/steering/structure.md, .specd/specs/aido-config/design.md | test/unit-paths | Root of a relative path; Root of an absolute path; every constructor called against an empty temp dir leaves it empty; no constructor creates a parent directory |
| ✅ T2 | craftsman | internal/config/config.go, internal/config/config_test.go | T1 | CGO_ENABLED=0 go build ./... && go vet ./... && go test ./... | R2.1, R2.2, R2.3, R2.4 | R2.1, R2.2, R2.3, R2.4 | feature | medium | standard | read,write | .specd/specs/aido-config/design.md, aido-blueprint-v1.0.md §4.3 | test/unit-config-load | absent config.yaml matches errors.Is(err, fs.ErrNotExist); empty file parses to zero config without error; malformed YAML error names file and position; unknown top-level key is ignored, not fatal; omitted optional key stays at zero value |
| ✅ T3 | craftsman | internal/config/validate.go, internal/config/validate_test.go | T2 | CGO_ENABLED=0 go build ./... && go vet ./... && go test ./... | R3.1, R3.2, R3.3, R3.4, R3.5 | R3.1, R3.2, R3.3, R3.4, R3.5 | feature | high | standard | read,write | .specd/specs/aido-config/design.md, .specd/steering/tech.md | test/unit-validate | missing project alone; missing tracked_branch alone; both missing at once; default_provider absent from providers map; provider name outside the supported set; required_docs entry not under okf/; three violations at once all reported in one error; a fully valid config returns nil |
| ✅ T4 | craftsman | internal/config/write.go, internal/config/write_test.go | T1 | CGO_ENABLED=0 go build ./... && go vet ./... && go test ./... | R5.1, R5.2, R5.3, R5.4 | R5.1, R5.2, R5.3, R5.4 | feature | high | standard | read,write | .specd/specs/aido-config/design.md, .specd/steering/tech.md T10 | test/unit-atomic-write | write to a fresh path; overwrite an existing file; failure before rename leaves destination bytes identical; failure before rename leaves no temp file in the directory; temp file is created in the destination directory, not TMPDIR; mode 0600 is honoured on create; destination directory missing is an error, not a silent mkdir |
| ✅ T5 | craftsman | internal/config/secrets.go, internal/config/secrets_test.go | T2, T4 | CGO_ENABLED=0 go build ./... && go vet ./... && go test ./... | R4.1, R4.2, R4.3, R4.4, R4.5, R4.6 | R4.1, R4.2, R4.3, R4.4, R4.5, R4.6 | feature | critical | deep | read,write | .specd/specs/aido-config/design.md, .specd/steering/product.md P7, .specd/steering/tech.md T9 | test/unit-key-resolution | env var set wins over secrets file; env var set but empty falls through to secrets file; secrets file absent yields ErrKeyNotFound not an I/O error; api_key_source none yields empty key and nil error; unparseable secrets file error names the path and quotes no file content; the key literal appears in no returned error string; provider absent from config yields a named error |
| T6 | craftsman | cmd/aido/main.go, cmd/aido/config_show.go, cmd/aido/config_show_test.go | T3, T5 | CGO_ENABLED=0 go build ./... && go vet ./... && go test ./... | R6.1, R6.2, R6.3 | R6.1, R6.2, R6.3 | integration | medium | standard | read,write | .specd/specs/aido-config/design.md, .specd/steering/product.md P5 | test/integration-cli-config-show;integration | valid config prints and exits zero; missing config prints to stderr and still exits zero; invalid config prints every validation problem and still exits zero; provider lines print api_key_source verbatim; no resolved key value appears anywhere in stdout or stderr |
| T7 | craftsman | internal/config/imports_test.go | T5 | CGO_ENABLED=0 go build ./... && go vet ./... && go test ./... | R1.1 | R1.1 | test | medium | standard | read,write | .specd/steering/tech.md T1, T3, T5, T7 | test/unit-import-allowlist | internal/config imports nothing outside the stdlib and gopkg.in/yaml.v3; no net/http import; no cgo import; the test fails when a disallowed import is added |
| T8 | auditor | .specd/specs/aido-config/review.md | T6, T7 | printf ok | R1.1, R1.2, R1.3, R2.1, R2.2, R2.3, R2.4, R3.1, R3.2, R3.3, R3.4, R3.5, R4.1, R4.2, R4.3, R4.4, R4.5, R4.6, R5.1, R5.2, R5.3, R5.4, R6.1, R6.2, R6.3 | R1, R2, R3, R4, R5, R6 | review | high | standard | read | .specd/specs/aido-config/requirements.md, .specd/specs/aido-config/design.md, the full diff of T1-T7 | review/aido-config-audit | every acceptance criterion traced to the test that demonstrates it; invariants I1-I6 each named with the check that holds them; any criterion asserted but not demonstrated reported as a finding, never marked passing |
